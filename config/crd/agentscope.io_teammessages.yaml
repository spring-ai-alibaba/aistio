---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: teammessages.agentscope.io
spec:
  group: agentscope.io
  names:
    kind: TeamMessage
    listKind: TeamMessageList
    plural: teammessages
    shortNames:
    - tm
    singular: teammessage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.teamRef
      name: Team
      type: string
    - jsonPath: .spec.from
      name: From
      type: string
    - jsonPath: .spec.to
      name: To
      type: string
    - jsonPath: .status.delivered
      name: Delivered
      type: boolean
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TeamMessage is a message in a team's distributed outbox.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TeamMessageSpec defines the desired state of a TeamMessage.
            properties:
              content:
                type: string
              from:
                type: string
              kind:
                type: string
              nonce:
                type: string
              teamRef:
                type: string
              to:
                type: string
            required:
            - content
            - from
            - teamRef
            type: object
          status:
            description: TeamMessageStatus defines the observed state of a TeamMessage.
            properties:
              attempts:
                format: int32
                type: integer
              delivered:
                type: boolean
              deliveredAt:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
